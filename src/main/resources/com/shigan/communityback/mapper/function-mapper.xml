<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shigan.mapper.FunctionMapper">
    <resultMap id="functionMap" type="function">
        <id property="id" column="id"></id>
        <result property="functionname" column="functionname"></result>
        <result property="url" column="url"></result>
    </resultMap>


    <select id="getFunction" parameterType="java.lang.Integer" resultMap="functionMap">
        SELECT * FROM FUNCTION WHERE id IN (SELECT functionid FROM userrole WHERE roleid=#{roleid})
    </select>
</mapper>